<!DOCTYPE html>
  <html>
  <head>
      <title>Update page</title>
      <style>
      table.center {
            margin-left: auto;
            margin-right: auto;
          }
      </style>
  </head>
  <body>
    <form method="post">
      <fieldset>
        <legend>Update Employee</legend>
        <table class="center">
          <tr>
            <td>Employee ID </td>
            <td>:<%= user.id %></td>
          </tr>
          <tr>
            <td>Name </td>
            <td><input type="text" value="<%= user.name %>" required="required" name="name"></td>
          </tr>
          <tr>
            <td>UserName</td>
            <td><input type="text" value="<%= user.username %>" required="required" name="username"></td>
          </tr>
          <tr>
            <td>Password</td>
            <td><input type="password" value="<%= user.password %>" required="required" name="password" ></td>
          </tr>
          <tr>
            <td>Phone</td>
            <td><input type="text" value="<%= user.phone %>" required="required" name="phone"  id="phone"></td>
          </tr>
          <tr>
            <td>Designation:</td>
            <td><input type="text" value="<%= user.designation %>" required="required" name="designation" ></td>
          </tr>
          <tr>
            <td></td>
            <td><button type="submit" name="choice" value="Update">Update</button></td>
          </tr>
        </table>
    </fieldset>
  </form>
  <table class="center">
      <tr>
        <td><a href="/admin/AllEmpList">Back</a>|
      	<a href="/logout">Logout</a></td>
      </tr>
  </table>
</body>
</html>